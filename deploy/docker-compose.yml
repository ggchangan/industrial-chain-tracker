services:
  chain-tracker:
    image: ccr.ccs.tencentyun.com/ygys30ds/industrial-chain-tracker:${IMAGE_TAG:-latest}
    container_name: chain-tracker
    env_file:
      - path: ../.env
        format: raw
    environment:
      NODE_ENV: production
      PORT: 4173
      HOST: 0.0.0.0
      DATA_DIR: /data
      STATE_STORE_DRIVER: ${STATE_STORE_DRIVER:-file}
      OBJECT_STORAGE_DRIVER: ${OBJECT_STORAGE_DRIVER:-local}
    ports:
      - "127.0.0.1:4173:4173"
    restart: unless-stopped
    init: true
    volumes:
      - chain-data:/data
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "-", "http://127.0.0.1:4173/api/v1/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  chain-data:
    name: industrial-chain-tracker-data
